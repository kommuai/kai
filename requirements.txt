# Kai — single dependency lockfile (runtime, integrations, dev/CI)

# --- Core (chat API + workspace-driven ReAct) ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pytz==2024.1
python-dotenv==1.0.1
PyYAML>=6.0.1
requests==2.32.3
openai>=1.30.0

# --- Integrations (Sheets, Qdrant, visitor-pass OCR plugin) ---
qdrant-client>=1.10.0
google-auth>=2.35.0
google-api-python-client>=2.151.0
ddddocr>=1.5.6

# --- Development / CI ---
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.0
