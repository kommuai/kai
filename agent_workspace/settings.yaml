# Non-secret defaults (env vars override). See docs/OPERATOR.md and docs/SETUP.md

session:
  idle_hours: 24
  max_history_messages: 100

agent:
  max_steps: 8
  footer_history_threshold: 10

channels:
  whatsapp:
    max_reply_chars: 4096

faq_learn:
  use_queue: true
  legacy_append: false

compile:
  extra_artifacts: false
